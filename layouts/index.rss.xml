{{/* regexp begin */}}
{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}

{{ $gphoto := `(<img src="(https://drive.google.com/open.+id=([^\"]+))".+alt="google-([^\"]+)" />)` }}
{{ $newgphoto := `
<figure class="gphoto-media">
  <a href="${2}" target="_blank" rel="noreferrer noopener">
    <img src="https://drive.google.com/uc?export=view&id=${3}" alt="${4}" title="Open in the new tab"></img>
  </a>
  <figcaption>
    <center class="caption-text">${4}</center>
  </figcaption>
</figure>
` }}
{{ $brs := `<br>` }}
{{ $clean := `` }}
{{ $codes := `(?ms:<div class="highlight">\s*<pre.*?<\/code>\s*<\/pre>\s*<\/div>)` }}
{{ $tablecodes := `(?ms:<div class="highlight">\s*<div.*<table.*?<\/table>\s*<\/div>\s*<\/div>)` }}
{{ $nocode := `<p>[see code by visiting website]</p>` }}
{{ $yts := `(?ms:<div class="mb4".*?<iframe.*<\/iframe>\s*</div>)` }}
{{ $iframes := `(?ms:<iframe.*<\/iframe>)` }}
{{ $footnotes := `(?ms:(<a href="#fnref.*?</a>)|(<sup id="fnref.*</sup>))` }}

{{/* regexp end */}}

<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ if eq  .Title  .Site.Title }}{{ .Site.Title }}{{ else }}{{ with .Title }}{{.}} on {{ end }}{{ .Site.Title }}{{ end }}</title>
    <link>{{ .Permalink }}</link>
    <description>Recent content {{ if ne  .Title  .Site.Title }}{{ with .Title }}in {{.}} {{ end }}{{ end }}on {{ .Site.Title }}</description>
    <generator>Hugo -- gohugo.io</generator>{{ with .Site.LanguageCode }}
    <language>{{.}}</language>{{end}}{{ with .Site.Author.email }}
    <managingEditor>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</managingEditor>{{end}}{{ with .Site.Author.email }}
    <webMaster>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</webMaster>{{end}}{{ with .Site.Copyright }}
    <copyright>{{.}}</copyright>{{end}}{{ if not .Date.IsZero }}
    <lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>{{ end }}
    {{ with .OutputFormats.Get "RSS" }}
    {{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
    {{ end }}
    {{ range .Site.RegularPages }}
    <item>
      <title>{{ .Title }}</title>
      <link>{{ .Permalink }}</link>
      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      {{ with .Site.Author.email }}<author>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</author>{{end}}
      <guid>{{ .Permalink }}</guid>
      <description>
        {{ "<![CDATA[" | safeHTML }}
          {{ .Summary | replaceRE $gphoto $newgphoto | replaceRE $brs $clean | safeHTML }}
          {{ if and (isset .Params "image") (not (eq .Params.image "")) }}
            <p><img src="{{ .Params.image | absURL }}" /></p>
          {{ else }}
            <p><img src="{{ .Site.BaseURL }}/img/default-header-img.jpg" /></p>
          {{ end }}
          <p><a href="{{ .Permalink }}">Visit website to read the full article</a></p>
          {{ .Content | replaceRE $gphoto $newgphoto | replaceRE $brs $clean | replaceRE $codes $nocode | replaceRE $tablecodes $nocode | replaceRE $yts $clean | replaceRE $iframes $clean | replaceRE $footnotes $clean | safeHTML }}
        {{ "]]>" | safeHTML }}
      </description>
    </item>
    {{ end }}
  </channel>
</rss>